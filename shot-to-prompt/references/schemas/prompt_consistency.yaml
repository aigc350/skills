type: object

required:
  - scene_id
  - shots

properties:

  scene_id:
    type: string

  shots:
    type: array

    items:
      type: object

      required:
        - shot_id
        - prompt

      properties:

        shot_id:
          type: string

        prompt:
          $ref: "common.schema.yaml#/Prompt"